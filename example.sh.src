#!/usr/bin/env bash

export LOG_LEVEL=4
shource parse_args()
shource join()
shource auto_log()

parse_args bool ns:array arr:a create:b -- "${@}"

{

date
date
date
date
date
echo "bool: $bool"
echo "create: $create"
for x in "${!ns[@]}"; do
    echo "ns-$x: ${ns[$x]}"
done

echo "$( join " " "${arr[@]}" )"
} >&2

wait
